# *******************************************************************************
# Copyright (c) 2026 Contributors to the Eclipse Foundation
#
# See the NOTICE file(s) distributed with this work for additional
# information regarding copyright ownership.
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************
name: Rust Coverage
on:
  pull_request:
    types: [opened, reopened, synchronize]
  workflow_dispatch:

jobs:
  rust_coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.14.0

      - name: Run tests with LLVM coverage (source-based, line + branch)
        run: |
          set -euo pipefail
          bazel coverage --lockfile_mode=error --config=llvm_cov --build_tests_only -- //src/...

      - name: Generate coverage report
        env:
          # Gate carried over from the legacy blanket flow (--min-line-coverage
          # 44). The LLVM pipeline measures 74.4% raw lines at introduction
          # (untested files now count as exact 0%), so 44 stays a safe floor;
          # ratchet once a baseline is agreed.
          COVERAGE_THRESHOLD: "44"
        run: |
          set -euo pipefail
          bazel run --lockfile_mode=error @score_tooling//coverage:generate_coverage_html -- \
            --yaml tools/coverage/coverage_justifications.yaml \
            --testlogs-subdir src \
            --archive-dir coverage_artifact

      - name: Upload coverage HTML
        uses: actions/upload-artifact@v4
        with:
          name: rust-coverage-html
          path: coverage_artifact/
